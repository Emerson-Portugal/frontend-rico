import { VexRoutes } from '@vex/interfaces/vex-route.interface'
import { HomePageComponent } from './pages'

export const HOME_ROUTES: VexRoutes = [
  {
    path: '',
    component: HomePageComponent,
  },
]
